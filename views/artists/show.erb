<div class="content">

  <h1>Artist in Profile</h1>

  <p><img src="<%= @artist.photo %>" alt="<%= @artist.name %> photo"></p>
  <h2><%= @artist.name %></h2>
  <p><%= @artist.birth %> - <%= @artist.death %></p>
  <p>Nationality: <%= @artist.nationality %> </p>

</div>
